apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: tp2
  labels:
    app: nginx-pod
spec:
  containers:
    - name: nginx
      image: nginx:1.29.3-alpine-slim
      ports:
        - containerPort: 80
